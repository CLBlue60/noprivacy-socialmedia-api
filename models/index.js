// Import the User model
const User = require("./User");

// Import the Thought model
const Thought = require("./Thought");

// Export the User and Thought models for use in other modules
module.exports = { User, Thought };
